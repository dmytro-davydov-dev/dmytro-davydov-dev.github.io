import React from 'react';
import { Box } from '@mui/material';
import Menu from './Menu';

const Header = () => {
    return (
        <Box sx={{}}>
            <Menu />
        </Box>
    );
};

export default Header;